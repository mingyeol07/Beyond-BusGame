using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RankCheck : MonoBehaviour
{
    [SerializeField] private List<GameObject> checks = new List<GameObject>();
    private void Start()
    {
        for (int i = 0; i < checks.Count; i++)
        {
            
        }
    }
}
